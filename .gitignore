target/
!.mvn/wrapper/maven-wrapper.jar
!**/src/main/**/target/
!**/src/test/**/target/

.idea/
*.iml
*.ipr
*.iws
.vscode/
.settings/
.project
.classpath

.env
.env.local
*.log

# Terraform
infra/.terraform/
infra/.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfplan
crash.log

# Ansible
ansible/inventory.ini
ansible/*.retry

# Reports
dependency-check-report.*

# OWASP Dependency-Check NVD database (pom.xml: nvd.data.directory).
# Hundreds of MB of H2 data rebuilt from the NVD feed and restored by the
# CI cache - never a repository artifact.
.nvd-cache/

# Image tarball produced by the docker_build stage and shipped to the host
# as a workflow artifact.
owaspcheck-image.tar.gz
