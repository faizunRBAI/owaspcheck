# Managed by Ansible - do not edit on the host.
# Mode 0600, root-owned. Values originate from CI repository secrets and the
# terraform state; they are never committed to the repository.

DB_HOST={{ db_host }}
DB_PORT=5432
DB_NAME={{ db_name }}
DB_USER={{ db_user }}
DB_PASSWORD={{ db_password }}

SERVER_PORT={{ app_port }}

JWT_SECRET={{ jwt_secret }}
JWT_EXPIRATION_MS=3600000

SPRING_PROFILES_ACTIVE=production
TZ=UTC
